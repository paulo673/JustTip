services:
  web:
    build:
      context: src/JustTip.Web
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://localhost:5000/api
    container_name: justtip-web
    ports:
      - "3000:80"
